<h3> Current Users - <%= link_to "Add User", new_admin_user_path %></h3>
<ul>
  <% @users.each do |user| %>
    <li> <%= user.email %> 
         <%= link_to "Edit User", edit_admin_user_path(user) %> |
        <%= link_to "Delete User", admin_user_path(user), :method => :delete,
:confirm => "Are you sure you want to delete this User?" %><br />
  <% end %>