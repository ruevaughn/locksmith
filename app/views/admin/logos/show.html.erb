<h2>
	<%= @logo.name%>
</h2> 
<%= image_tag @logo.image_url.to_s %>

	<%= link_to "Edit Logo", edit_admin_logo_path(@logo) %> |
  <%= link_to "Delete Logo", admin_logo_path(@logo), :method => :delete, :confirm => "Are you sure you want to delete this Logo?" %> 
